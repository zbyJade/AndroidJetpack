<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="ShoeTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/shoe_theme_color</item>
        <item name="colorPrimaryDark">@color/shoe_theme_color</item>
        <item name="colorAccent">@color/shoe_theme_color</item>
        <item name="android:textColor">@color/shoe_theme_color</item>
    </style>

</resources>
